---
name: feedback-terse
description: User prefers concise responses, no trailing summaries, no emoji
metadata:
  type: feedback
---

Keep responses short and direct. No trailing summaries after code changes — user reads diffs. Never use emoji unless explicitly asked. One sentence per update is almost always enough.
