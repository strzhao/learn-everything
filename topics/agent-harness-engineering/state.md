---
topic: 构建 AI Agent Harness（以 Claude Code 为参照系，从 0 到 1）
slug: agent-harness-engineering
status: active
stuck_count: 0
created_at: 2026-05-21T06:50:43+00:00
updated_at: 2026-05-30T18:00:00+08:00
bloom_level: create
artifact_count: 12
---

## 当前位置

bloom: create | 已覆盖: harness≠APIcall, agent loop 三角骨架, 观察→反馈→决策闭环, stop_reason 开关, 协议约束塑造行为, Tool 三件套(schema+executor+permission), production 双层 permission(harness gate 正交 ask tool), prompt injection 证伪 v1, is_error 字段下 model 自适应, bypassPermissions 5 条推测全部对照工业源码验证, permission mode = (tool×mode)→policy 矩阵, mode 矩阵物理承载 = 有序 if 链 + 早返回 + decisionReason 多维传出, dispatch+is_error 是判决+反馈两层职责, sandbox+permission 双层防御针对不同攻击向量, policy 是 runtime 行为/config 是行为参数化输入, hard-block 防 user 误开/与 sandbox 正交, 三层 handler 工厂模式分离 policy 决定 vs ask 执行, 工业 mode 不止 3 种(acceptEdits/auto/bypassPermissions/default/dontAsk/plan), multi-agent 必要性(单 agent context 爆/思考链断/责任模糊), coordinator/worker 二分, 三种 worker 形态(interactive/swarm/coordinator-as-subagent), swarm worker 无 ask_user 是物理约束(不是任务限制), policy 矩阵加 agent-role 维度变 (tool×mode×agent-role), multi-agent 把 context 问题从深度变广度, **swarm worker 的 ask 实际是路由到 leader (mailbox + permission callback)，不是 deny**, **context 压力两种形态(深度爆/广度爆) 同源同解**, **token 经济本质 = messages 每轮重传线性成本 + prompt cache 失效**, **三种 compaction 策略各有边界**, **compaction 是独立 sub-system 不是 agent 自身职责**, **hook 系统 = 事件总线 + callback 注册中心 (pub/sub)**, **3 种 hook handler 执行形态**, **hook ≠ sub-system 替代品**, **sub-system 入口 ≠ hook emit point**, **三种 telemetry 形态分工 = logs/metrics/traces**, **单一入口 + 多渠道 fan-out**, **cardinality 控制**, **privacy by default**, **streaming = pipelining**, **yield order ≠ concat order**, **hook 必须立刻决定**, **协议层 id 配对让顺序自由**, **StreamingToolExecutor addTool 立即启动 + isConcurrencySafe 并发控制 + abort/discard 机制**, **v7 所有 sub-system 在 streaming 模式下零修改可用（架构正交性验证）**, **MCP = tool 定义权从 harness 内部移到外部 server（USB 协议类比）**, **MCP tool 与内置 tool 同权 = 复用 dispatch/permission/hook/obs 全管道**, **工业 MCP 3 层架构 = 传输层(MCPConnectionManager 1141行) + 协议层(client.ts JSON-RPC) + 集成层(merge 进 tools.ts 注册表)**, **手写 JSON-RPC = id 配对(Map) + 单一 30s 超时 + newline-JSON + initialize lifecycle (砍掉 SDK Zod/重连/多 transport/Progress callback/Elicitation/三层 timeout)**, **MCP tool dispatch 同权物理实现 = mcp__ 前缀 merge + execute startsWith 分支 (仅穿过最小切面 / v8 §6-15 字面 0 修改)**, **架构正交性第三次验证 (hook/permission/obs 对 MCP tool 字面 0 修改自动触发 / v7 obs 完全不知 MCP 存在却自然命中 cardinality 控制)**, **initialize lifecycle 不能跳过 = protocol 版本协商 + capability negotiation (HTTP/2 SETTINGS / TLS ClientHello 同源)**, **MCP inputSchema = JSON Schema 零转换 Anthropic API 接受 (provider-agnostic 设计)**, **MCP 路径注入陷阱 = MCP server 内部安全是 server 实现者责任 (与 task 02 sandbox+permission 双层防御同源 / client 侧 gate 是补充不是替代)**, **system prompt = 动态装配产物不是静态字符串**, **SYSTEM_PROMPT_DYNAMIC_BOUNDARY sentinel string 是下游 cache 物理分割点 (splitSysPromptPrefix / buildSystemPromptBlocks 消费) 让前缀部分 scope:global 跨组织共享 + 后缀部分会话隔离**, **systemPromptSection memoization 默认开 + DANGEROUS_uncachedSystemPromptSection 显式 opt-out (cache 默认安全的工业设计)**, **_reason 下划线参数 = 类型系统强制 self-audit disclaimer (review-time 强制 runtime 不消费 / 跟 v6 hook handler 必填 reason 同源)**, **compact 副作用 clearSystemPromptSections = compact 后语义状态变 section compute 可能依赖旧上下文 → cached value stale → 必须丢 cache 强制重算 (v5 没看到的"语义因果链")**, **MCP cache-break 是 v9 同权隐藏代价 (mcp_instructions DANGEROUS_uncached reason: "MCP servers connect/disconnect between turns") = 同权只是 dispatch 路径同 cache 经济上付费**, **system prompt 子系统化 (sectionCache Map module-scoped + 2 工厂 + resolveSystemPromptSections + clearSystemPromptSections + assembleSystemPrompt 返回 prefix/suffix 两段)**, **cacheBreak: true DANGEROUS 跳读 cache 但仍写副本 (不是"不缓存"是"永远新鲜" / 工业字面 1:1)**, **clearSystemPromptSections 放 maybeCompact 末尾不是开头 (避免浪费已建立的 cache / 工业 postCompactCleanup.ts:62 同位置)**, **lesson 10 修正 1: BOUNDARY 之后 cacheScope:null 是"不进 cache"不是"会话隔离的小 cache"**, **lesson 10 修正 2: DANGEROUS 仍写 cache 副本只跳读 (resolveSystemPromptSections line 53-54 字面)**, **v10 偏离工业的教学化简: BEFORE_BOUNDARY 放 5 dynamic section 让 cache 命中可视化 / 工业 dynamicSections 全在 AFTER**, **架构正交性第四次验证: v10 新增 §19/§20/§21 共 159 行不动 v9 §1-18 任何核心 (compact §7 加 1 行 clear / runRounds §10 改 system 签名为 assembler)**, **skill = 运行时行为策略注入 (新 tool 定义 + skill 正文走 user-role text 注入 messages 流 + 临时 allowed-tools merge / dispatch/hook/permission/obs/cache 全部字面不动)**, **skill_listing 走 attachment 通道 = per-turn 增量注入 user message 前 prepend <system-reminder>...</system-reminder> 文本块 (首轮全列 / 后续轮 sentSkillNames Set delta-dedup / 工业 attachments.ts:2661 + messages.ts:3097 wrapInSystemReminder) / 不是 system prompt section 因为 dynamic 内容不锁死在 cacheable system prompt 跟 v10 mcp_instructions 选 DANGEROUS_uncached 同源**, **inline vs fork = context 隔离取舍 (inline = skill 内容注入主对话流 model 接续工作 / fork = 子 agent 独立 context 跑完只返回摘要避免大量 tool_result 污染主 agent) 跟 task 05 context-compactor 同源动机**, **shell 模板加载期 vs 运行期正交 (shell 永远在主 agent 进程 getPromptForCommand 内部跑 + 临时 alwaysAllow merge allowed-tools 期间生效 / fork 是后续把成品 task 传给 worker 的运行期隔离 / 两层 allowed-tools merge: 第一次给 shell 模板 第二次给 fork 子 agent 的 permissionContext)**, **MCP skill 强制跳过 shell 执行 = untrusted remote 安全边界 (loadSkillsDir.ts:371-374) 跟 task 02 双层防御 + task 09 路径注入陷阱同源**, **TodoWrite = 极简 state-set tool (116 行) + 184 行 prompt + 三层 reinforcement schedule (continuous tool_result 钉子 / fixed-interval 每 10 轮 todo_reminder attachment / event-triggered verificationNudgeNeeded) — Skinner schedules of reinforcement 工业落地 / Vygotsky metacognitive scaffolding / Grassé stigmergy 三种理论同源现象**, **TodoWrite 不变量 (exactly ONE in_progress / completed 即清空) 是 prompt 协议而非 runtime validation 强制 (call() 函数零 validation / TodoListSchema 无 list-level refinement / 唯一约束在 prompt.ts:158 字面) — LLM agent 工程独特的"软契约"现象 (与 v6 hook reason / v10 cacheBreak / v11 INLINE_PATTERN 同源)**, **verificationNudgeNeeded = prompt 协议同源实例 (text nudge in tool_result 而非 runtime spawn block) — 保留 model agency + 让 model 学到为什么 + 可类推未来场景**, **TodoWrite per-agent 隔离 (agentId ?? sessionId) — 与 v4 multi-agent context 二分同源 / 每个 sub-agent 自有 todo / sub-agent 不污染主 agent 状态空间**, **TodoWrite session 持久化无文件 (v1 transcript-derived: sessionRestore.ts:77-93 倒扫 messages 找最后一个 tool_use 反序列化 input.todos) — messages 数组即数据库 / v5 compaction 同精神**, **v12 mini TodoWrite 6 论断全部字面命中工业**: (1) 极简 tool ~25 行 + 协议浓缩进 description; (2) 软契约 schema 无 list-level refinement / in_progress>1 只 audit; (3) 三层 reinforcement 字面同步工业 (continuous "Todos have been modified successfully" + fixed-interval "The TodoWrite tool hasn't been used recently" + event-triggered "NOTE: You just closed out 3+ tasks"); (4) per-agent 隔离 todoKey = agentId ?? SESSION_ID; (5) extractTodosFromTranscript 倒扫还原无文件; (6) **架构正交性第六次验证** = OBS METRIC dump 字面 obs 不知 TodoWrite 存在却自动 cardinality-control (tool_name=TodoWrite count=4 同 read_file/edit_file 同管道) + compaction 照常清 TodoWrite tool_result. **踩坑修正 2 处**: auto-allow 路径漏传 agentId (replace_all 命中不全 / live swarm run-log 抓现行) + 单一阈值 vs 工业双阈值教学化简. **6 处教学偏离 disclaimer 显式标注** (协议放 description / 184→45 行浓缩 / piggyback reminder / spawn_swarm 替 VERIFICATION_AGENT / 略 feature flag / 不做 v2), **lesson 4 命名修订 (2026-05-30): 04-coordinator-swarm → 04-subagent-fork** (源码 ground truth 读取确认: 工业 "swarm" = "team" = 多进程协作系统/`utils/swarm/constants.ts:2 SWARM_SESSION_NAME='claude-swarm'`/`TeamCreateTool.searchHint='create a multi-agent swarm team'` 字面; lesson 13 主题). **v4 实际是 fork 同进程 sub-agent ≈ 工业 AgentTool (subagent_type=general-purpose)**，跑独立 messages 数组、同步返回 summary string。**`"swarm-worker"` role 字面 + `spawn_swarm` 工具名 + `swarmId` 保留作 historical code identifier** (5-12 ts 共享，改 role 名会让继承代码全断)；叙述层读作"fork 出的 sub-agent"。**v4 vs 工业 team 5 件套差距**: (1) v4 无文件持久化 / 工业 team `~/.claude/teams/{team}/config.json`; (2) v4 同进程 async fork / 工业 team 多进程 (tmux pane/iTerm2 split/InProcessTeammateTask backend); (3) v4 同步函数返回 summary / 工业 team 文件锁 mailbox `.claude/teams/{team}/inboxes/{name}.json` 异步; (4) v4 spawn_swarm 同步传 task 字符串 / 工业 team 共享 task list + task owner 认领 ("Team = TaskList"); (5) v4 leader 同步等 / 工业 team idle 状态机 + automatic message delivery + shutdown protocol

**Socratic 05 评分**：Q1（naive truncation 反而比 hybrid 更合适的场景）学生选"CLI 短任务"——抓住了 hybrid 隐藏成本（额外 model call + 延迟）；Q2（compaction 该由谁负责）学生选"harness 层独立 sub-system"——跟工业 `src/services/compact/` 完全一致。两题全对无需补丁，create 层稳定。

**Source-reading 自我修正记录**：在 lecture 04 后讨论中，学生质疑我"swarm 自动 deny"论断，主动用 Explore 读 `src/hooks/toolPermission/handlers/swarmWorkerHandler.ts` 验证。源码证据推翻了 instructor 论断 —— swarm 的 ask 实际是通过 mailbox 路由到 leader，由 leader 决定 allow/reject。学生 v4 makeRoutedAsk 设计反而跟工业 mailbox + permission callback **同精神**，是工业实现的 in-process 简化版。这次自我修正是 create 层"拒绝接受未经源码验证的论断"判断力的高光。

**Socratic 06 评分**（Lecture 05 细化版后置，task 05 入口前置）：Q1（压缩最小单元）选"整个 messages 数组原子压" ❌ → 收紧 "messages[i] vs messages[i+1] 协议捆绑" 场景题答对 ✅；Q2（压缩免疫集）选"所有 tool_result 不可压" ❌ → 收紧 "10KB 原文已被 round 7 model reasoning 融入" 场景题答对 ✅；Q3（压缩执行者）选"专用 compaction LLM call" ✅ 一次到位；Q4（multi-agent compact 原则）选"swarm 短不需要 compact" ❌ → 收紧 "30 轮 + 80K tokens 长任务 swarm" 场景题答对 ✅。三错共性：协议约束忽略 + 应该有等同于原文必留 + 任务驱动而非架构驱动 —— 收紧场景题逼出三层盲区。补丁要点：create 层"看具体场景验证抽象"判断力进一步强化。

## 下一步建议

**Task 05 验收**：v5 context compactor 全套交付（330 行 / 10 段 / 严格不动 v4 dispatch / 4 份 run-log 含 micro+full+swarm-internal 实测）+ notes.md 20KB 8 节 + excerpts.md 7 段源码 + lesson.md 17KB 13 段（agent-notebook 28 blocks 全命中）+ context 隔离实测强证（coordinator FINAL 段 2078 chars 完全不含 COMPACTED SUMMARY / Old tool result content cleared / swarm-worker）。4 条要点全部 ✅ 命中工业实现。v5 第一版 fullCompact 因悬空 tool_result 被 API 报错 → 修复加 firstValid 跳过逻辑，这条 bug 与修复本身已沉淀为要点 1 "round 原子单位" 的工程实证。bloom 保持 create（第三次巩固完成）。

## 下一步建议

**Socratic 07 评分**（Lecture 06 后置 / task 06 入口前置）：Q1（hook 替代 permission/compact 的隐患）选 "Hook handler 不可靠" ✅ —— 抓住核心 distinction：sub-system 是 in-process critical path 必须可控，hook 是 cross-cutting 旁路广播可以失败。跟 task 02 sandbox+permission 双层防御针对不同攻击向量同源。Q2（v5 maybeCompact 对应哪个 hook event）选 "两者都不是" ✅ —— 揭示 sub-system 入口 vs hook emit point 的精确层次：maybeCompact 是 sub-system 入口，PreCompact/PostCompact 是 sub-system 内部 emit 的 event，两者叠加不替代。也回答了 v5 notes.md §7.2 设计悬念：工业 runRounds 加 hook 链不是替代 maybeCompact 而是在其内部 emit 标准 event。两题全对无补丁，create 层稳固巩固。

**Task 06 验收**：v6 mini hook engine 全套交付（434 行 / 11 段 / v5 任何核心不动 / 4 份 run-log 含 failing-hook 实测核心未被阻断）+ notes.md 18.8KB 7 节 + excerpts.md 8 段源码引用 + lesson.md 15.2KB 13 段（agent-notebook 20 blocks 全命中）。4 条要点全部 ✅ 命中工业实现。**CLAUDE.md 0 假设原则严格执行**：所有 file:line 都来自 Explore agent 全扫源码后验证（HOOK_EVENTS 27 项 coreTypes.ts:25-53 / Promise.allSettled AsyncHookRegistry.ts:144 / 3 handler 60s/10min/30s timeout 各异 / ssrfGuard.ts:5-40 IP block 列表含云元数据 169.254.169.254）—— 与 task 05 apiMicrocompact 凭命名猜错反例完全不同。bloom 保持 create（第四次巩固完成）。

## 下一步建议

**Socratic 08 评分**（Lecture 07 后置 / task 07 入口前置）：Q1（哪个字段必须 events 不能 metrics）选 "tool_input.path 任意文件路径" ✅ —— 抓住"不可数高基数 = metric 死敌"工程直觉，正确排除 30 个工具名干扰项（30 个时序对 metric backend OK，关键是可预测上限）。Q2（SLO 30 秒定位 cause 的 telemetry 配置）选 "logs+metrics+context map 三层组合" ✅ —— 精准对应 SLO 三种隐含需求：metrics 触发告警 / logs 定位细节 / context map 同进程查询。lecture 07 三形态分工核心论断的具体应用。两题全对无补丁，create 层第五次巩固完成。

**Socratic 09 评分**（task 07 入口前置细化版）：4 题深入 v7 实现 4 个具体决策点。Q1（fan-out 单一入口必要性）选"语义一致性 + 字段标准化 / 分散导致 N 调用点不一致" ✅；Q2（5 字段 cardinality 精确划分）选"metric label = tool_name+role+decision；events only = prompt_id+file_path" ✅ 精确对应 events.ts:49,56-61 注释；Q3（privacy 实现层）选"机制层 1 处 vs 业务层 N 处 / **与 task 02 v1 同源失败模式**" ✅ 主动联想；Q4（context map vs sink）选"同步 inspect vs 异步导出 / 消费时机不同决定三者必须并存" ✅。**4 题全对一次通过 + 主动联想 task 02 同源** —— create 层第五次巩固稳。

**Task 07 验收**：v7 mini observability sub-system 全套交付（545 行 / 13 段 / v6 §1-10 字面量不动 / 4 份 run-log 含 cardinality 拒绝 + privacy redact 对比实测）+ notes.md 19KB 6 节 + excerpts.md 7 段源码（含 permissionLogging.ts:178-235 fan-out 教科书 + events.ts:13-19 redact 字面量 + :49 cardinality 注释）+ lesson.md 15.9KB 14 段（agent-notebook 14 blocks 全命中）。**修复关键 bug**：`Bun.file().writer()` truncate 改 `appendFileSync` —— sink 内部必须 append。**CLAUDE.md 0 假设原则深化执行**：所有字面量证据（OTEL_LOG_USER_PROMPTS / <REDACTED> / "Single entry point" / "filesystem paths too high-cardinality"）来自实际 Read 验证。4 条要点全部 ✅ 命中工业实现。bloom 保持 create（第五次巩固完成）。

**Socratic 10 评分**（Lecture 08 后置 / task 08 入口前置）：Q1（yield order ≠ concat order 实战陷阱）选"A 比 C 慢因为 Post{A} 在 Pre{C} 之后"❌ 推断错误 ✅（学生正确识别这是错误推断，完成顺序 ≠ 执行耗时，需 wallclock）；Q3（streaming 核心延迟收益）选"pipelining" ✅ 一次到位，正确排除 SSE multiplexing / async generator / 多 tool 并发三个干扰项；Q2（streaming 下 hook 假设）选"不能假设上一个 handler 已返回"❌ → 真正错的是 D"可以等到 stop_reason 后再决定"。盲区：把 hook 并发触发误判为危险 / 把延迟决策误判为可行。收紧 Q2 场景题"v6 Promise.allSettled 实际行为"选"3 handler 并发触发 + dispatch 等所有 settle + 互不感知" ✅ 一次到位 —— Promise.allSettled 语义内化。**create 层第六次巩固完成**。

## 下一步建议

**Task 08 验收**：v8 mini streaming agent 全套交付（593 行 / 15 段 / v7 §1-13 字面不动仅 §5 加 --sim-delay 分支 + §10 加 streaming 分支 / 4 份 run-log 含 yield-order-proof + batch-vs-stream-wallclock）+ notes.md 4.4KB 6 节 + excerpts.md 5.4KB 7 段源码（含 StreamingToolExecutor.ts:40-62 class 定义 + :76-124 addTool + grouping.ts:29-31 字面证据 + query.ts:838-862 streaming loop）+ lesson.md 7.7KB 14 段 + README.md 三段式。4 条要点全部 ✅ 命中工业实现。**API 配置修复**：langbase token 过期 → 改用 DeepSeek Anthropic 兼容端点（cc-switch SQLite 提取）。bloom 保持 create（第六次巩固完成）。

**Task 09 验收**：v9 MCP mini client 全套交付（730 行 / 18 段 / v8 §1-15 字面不动 仅 §1/§3/§5 共 3 处共 ~10 行 + §16/§17/§18 新增 ~120 行 / 2 份 run-log 含 weather-normal bypassPermissions+obs + calculator-denied default+用户 n 拒绝实测）+ mcp-mock-server.ts 54 行（initialize/notifications/initialized/tools/list/tools/call 完整 lifecycle + stderr 日志通道分离 + 路径注入陷阱 city=固定文本不读 fs）+ notes.md 6 节深度反思（手写 vs SDK / 同权 4 个决策 / initialize 不能跳的 3 个理由 / 路径注入与 task 02 同源 / 改动统计 / 工业对照速查 7 项）+ excerpts.md 7 段源码引用（StdioClientTransport spawn / fetchToolsForClient 同结构 / MCPTool.call() 无 isMcp 分支 / JSONRPCRequest spec / Tool spec / 三层 timeout / 重连状态机）+ lesson.md 14 段（USB 类比 + 三件套 transport + lifecycle 不可跳 + 同权 4 论断 + 路径注入与 task 02 同源 + 工业意义）。**4 条核心论断全部 ✅ 字面命中工业实现**：(1) MCP tool 与内置 tool 同结构 (Anthropic input_schema = MCP inputSchema 零转换) (2) dispatch 路径无 isMcp 特殊分支 (我们用前缀字符串 / 工业用对象多态本质一样) (3) hook/permission/obs 对 MCP tool 同样触发 (OBS METRIC dump 含 mcp__mock__ label) (4) MCP server 完全外部黑盒 (路径注入陷阱印证)。**CLAUDE.md 0 假设原则严格执行**：所有 file:line 标注 (StdioClientTransport 944-958 / fetchToolsForClient 1768-1815 / MCPTool.call 1833-1867 / JSONRPCRequest spec.types 129-150 / Tool spec 1172-1214)，三层 timeout 常量值 (DEFAULT_MCP_TOOL_TIMEOUT_MS = 100_000_000 / MCP_REQUEST_TIMEOUT_MS = 60000 / getConnectionTimeoutMs = 30000)，MAX_ERRORS_BEFORE_RECONNECT = 3 重连阈值——全部 Explore 验证。**架构正交性第三次验证完成**：v7 obs 完全不知 MCP 存在但 PreToolUse/PostToolUse 自然命中 + tool_name=mcp__mock__weather_lookup 完整 cardinality 控制 + calculator-denied 场景 is_error=true label 正确 propagate (v3 task 02 设计的反馈通道对 MCP 同源)。bloom 保持 create（第七次巩固完成 / 已稳定）。

**Socratic 12 评分**（Lecture 10 后置 / task 10 入口前置）：4 题全对一次过 + 主动联想 task 02 / v6 / v9。Q1（BOUNDARY 工业价值）选"下游 cache 物理分割点 + scope:global 跨组织共享 vs 会话隔离" ✅ 精准对应 prompts.ts:106-109 注释 + splitSysPromptPrefix/buildSystemPromptBlocks 消费点；Q2（_reason 下划线设计意图）选"类型系统强制 self-audit + 跟 v6 hook handler reason 同源" ✅ 主动联想 v6；Q3（clearSystemPromptSections 在 /compact 时被调用的原因）选"compact 后语义状态变 / section compute 依赖会话上下文 / cached value stale 必须丢 cache 强制重算" ✅ 完整说出语义因果链；Q4（v9 同权论断隐藏代价）选"dispatch 同权 ≠ cache 经济同权 / MCP cache-break 是 v9 没付出的账" ✅ 自己说出"同权只是 dispatch 路径同 cache 经济上付费"——这正是 lecture 10 概念地图里埋的最深钩子。bloom 保持 create（第八次巩固完成）。

**Task 10 下发**：v10 System Prompt Assembly Engine。v9 §1-18 字面不动 + 新增 §19 SystemPromptSection 子系统（systemPromptSection memoized 工厂 / DANGEROUS_uncachedSystemPromptSection 必填 reason / resolveSystemPromptSections Promise.all+cache 命中 / clearSystemPromptSections 在 maybeCompact 后调用）+ §20 prompt 段定义（core_instruction static / tool_list static / env_info per-session / mcp_instructions DANGEROUS_uncached / memory per-user / boundary 拼装）+ §21 BOUNDARY 切分逻辑（splitSysPromptPrefix 教学版 / audit 输出两段长度让学生眼见 cache 经济）+ 2 份 run-log（cache-warm 第二轮 OBS 显示 cache hit / compact-clear 触发后所有 section 重算）。教学复杂度 ~150-200 新增行（v9 730 → v10 ~900 行）。4 条核心论断对照工业：(1) memoization 默认开 + DANGEROUS opt-out 是 cache 默认安全的设计 (2) _reason 类型系统 self-audit (3) compact 副作用 clear section cache 是语义因果链 (4) MCP cache-break 是 v9 同权隐藏成本。spec 用 0 假设原则严格执行（所有 file:line 引用 prompts.ts:107-109 / 114-115 / 491-554 + systemPromptSections.ts:20-58 / 65-68）。

预判: 等 Task 10 v10 交付（system_prompt_assembly.ts ~200 行 + agent-v10-system-prompt.ts 继承 v9 ~900 行 + 2 份 run-log + lesson/notes/excerpts/README）| 备选 A: 学生执行中遇到 cache 边界设计困难 → 出 hint level 1 引用工业 splitSysPromptPrefix 切分位置 | 备选 B: 学生完成后 socratic 13 验证"四论断+v5/v9 衔接"理解

**Task 10 验收**：v10 System Prompt Assembly Engine 全套交付（889 行 / 22 段 / v9 §1-18 几乎不动 + §19/§20/§21 新增 ~159 行 / 2 份 run-log 含 cache-warm 跨多轮 5/6 hit + compact-clear "cleared by compact: 6 entries dropped" 字面命中）+ notes.md 6 节深度反思（工厂签名 + memoization 数据结构 + BOUNDARY 切分修正 + clear 末尾 vs 开头 + v9 同权债 + 改动+工业速查）+ excerpts.md 7 段源码引用（systemPromptSections.ts:20-58 + state.ts:203,1641-1654 + prompts.ts:114-115,491-555 + utils/api.ts:321-360 + postCompactCleanup.ts:31-62）+ lesson.md 14 段（USB chunk-splitting 类比 + 4 论断 + run-log 字面证据 + v9 同权债还账 + 工业偏离 disclaimer）+ README.md 三段式 + spec.md retrospective。**4 条核心论断全部 ✅ 字面命中**：(1) memoization 默认开 (cache-warm Round2+ hit=5/6) (2) DANGEROUS opt-out (mcp_instructions 始终 miss) (3) clearSystemPromptSections 在 maybeCompact 末尾 (字面 "cleared by compact: 6 entries dropped") (4) BOUNDARY sentinel 切分 (audit prefix=743 / suffix=98 跨轮稳定)。**CLAUDE.md 0 假设原则严格执行 + 主动修正 lesson 10 错误**：lesson 10 讲"DANGEROUS 不缓存"和"BOUNDARY 之后会话隔离"两处不精确，excerpts.md / notes.md 显式修正为"DANGEROUS 仍写 cache 副本只跳读" + "BOUNDARY 之后 cacheScope:null 不进 cache"。**架构正交性第四次验证**：v9 §1-18 几乎字面不动，§19/§20/§21 新增 + §7 加 1 行 + §10 runRounds 签名改 system → assembler 完成全部融入。**HTML 视图 13 blocks 全绿 (7 markdown + 3 code @include + 3 log @include + 0 error / 2 runs 全绿)**。bloom 保持 create（第九次巩固完成 / 已极稳定）。

预判: 等学生审阅 HTML 视图 → socratic 13 进一步巩固 / 出 task 11 (候选: skill 系统 / slash command / 子 agent 资源管理 / plugin / final 拼装) | 备选: 学生发现 v10 实现有偏离工业的地方 → 主动修正源码引用

**Socratic 13 评分**（task 11 实施前的概念前置验证 / Lecture 11 实质内嵌在源码深入阅读中）：4 题一次过 + Q3 主动联想 task 05 同源 + Q4 准确切分"加载期 vs 运行期"两个时序。Q1（skill 物理本质）选"新 tool 定义 + user-role text 注入 + 临时 allowed-tools merge / dispatch/hook/permission/obs/cache 全部字面不动" ✅ 抓住架构正交性第五次验证的内核；Q2（skill_listing 为何 attachment）选"per-turn 增量注入 / 首轮全列后续 delta / 不占 system prompt 固定带宽" ✅ 主动联想 v10 mcp_instructions DANGEROUS_uncached 同源动机；Q3（code-review skill inline vs fork）选"fork —— 大量中间 tool_result 占主 agent context / 跟 task 05 context-compactor 同源" ✅ 自主联想 task 05；Q4（git-summary shell 在哪个进程跑）选"主 agent 进程 getPromptForCommand 内部 / shell 期间临时 merge / 成品作 task 传 worker / shell 加载期 vs 运行期正交" ✅ 精准切分两个时序。**create 层第 10 次巩固**。spec.md 已被 instructor 主动修正 3 处工业偏离（论断 1/2/3 完整重写 + §25 注入位置改 attachment + v10 改造点表更新 §21 字面不动 / §10 加 system-reminder prepend）。

**Task 11 验收**：v11 mini skill system 全套交付（1101 行 ts / 22 段 / v10 §1-22 字面不动除 §3+15 / §5+5 / §10+10 / §22+4 共 4 处最小侵入 / §23-§26 新增 ~165 行 / 2 份场景驱动 run-log）+ skills/greeter/SKILL.md (inline) + skills/git-summary/SKILL.md (context: fork + allowed-tools: Bash(git:*)) + notes.md 8 节深度反思（改动统计 + 工业对照表 + 两层 alwaysAllow merge 精确路径 + Base directory 双重前缀踩坑修正 + INLINE_PATTERN 中文 punctuation 简化 + spawn_swarm 精神同构 + system-reminder tag 语义价值 + 第五次正交性物理证据）+ excerpts.md 8 段工业源码引用（loadSkillsDir:344-401 + promptShellExecution:49-143 + SkillTool:122-289 forkedAgent:147-232 + SkillTool:1095-1108 inline + attachments:2661-2751 + messages:3097-3099 + loadSkillsDir:407-480）+ lesson.md 13KB 12 段（VSCode extension 类比 + 5 论断 + 2 场景 run-log 字面证据 + 工业偏离 disclaimer）+ README.md 三段式。

**5 条核心论断全部 ✅ 字面命中**：(1) skill = 行为策略注入（greeter run-log Round 2 出现"嘿！见到你..."问候头证明 model 行为被 SKILL.md 改变 / dispatch/hook/permission/obs/cache 字面不动）(2) skill 正文走 user-role text 通道（greeter run-log tool_result.content 含完整 SKILL.md 正文）(3) skill_listing 走 attachment <system-reminder>（FINAL MESSAGES 首条 text block 含 [Available Skills] 列表）(4) shell 加载期 vs 运行期正交（git-summary run-log audit 序列字面证据 temp-allow restored 早于 spawn worker）(5) 架构正交性第五次验证（OBS METRIC tool_name=Skill role=interactive 完整 cardinality）。

**CLAUDE.md 0 假设原则严格执行 + 主动修正 spec.md 工业偏离**：spec.md 论断 1/2/3 完整重写（user-role text + attachment + 加载期 vs 运行期）+ §25 注入位置改 attachment + v10 改造点表更新（§21 字面不动 / §10 加 system-reminder prepend）—— 这 3 处修正全部来自工业 Read 验证（SkillTool.ts:1101-1108 inline 字面 / attachments.ts:2661-2751 字面 / messages.ts:3097-3099 字面 / loadSkillsDir.ts:344-401 字面）。**踩坑修正 2 处实测**：(1) Base directory 双重前缀（spec 示例错误抄写 / 工业 SKILL.md 不写这行）(2) INLINE_PATTERN 中文冒号 lookbehind 不匹配 → SKILL.md 加空格简化。**HTML 视图 15 blocks 全绿 (4 code + 3 log + 8 markdown + 0 error)**。bloom 保持 create（第十次巩固完成 / 已极稳定 / mini harness 7 大子系统齐备）。

预判: 等学生审阅 HTML 视图 → assemble 触发 final/ 拼装 11 个 artifact 作毕业证 | 备选: 学生发现 v11 实现有偏离工业的地方（如 INLINE_PATTERN 放宽到中文 punctuation 是否值得） → 主动修正

**Task 12 验收**：v12 mini TodoWrite 全套交付（agent-v12-todowrite.ts 1334 行 / v11 §1-26 字面不动除 §1+2 / §3+40 / §5+6 / §10+8 共 4 处最小侵入 + §27 executeTodoWrite ~45 行 / §28 maybeInjectTodoReminder ~55 行 / §29 extractTodosFromTranscript + restore/isolation demo ~75 行）+ 3 份场景驱动 run-log（lifecycle 真实 LLM 8 轮完整生命周期含 verify nudge / fixed-interval 缺席探测 sinceWrite=1→2→3 + system-reminder 字面注入 / restore-isolation 确定性 demo 含修复 auto-allow bug 后实测 swarm[0] 隔离）+ notes.md 7 节深度反思（改动统计 + 6 论断对照工业 + 3 层 reinforcement 理论同源 + 软契约现象 + 2 踩坑修正 + 6 处教学偏离 disclaimer + 工业速查）+ excerpts.md 7 段工业源码（TodoWriteTool.ts:65-103/104-113 + types.ts + prompt.ts:144-160 + attachments.ts:254-257/3296-3314 + messages.ts:3663-3679 + sessionRestore.ts:77-93）+ lesson.md 8.5KB 按 8 条叙事规则严格执行（115/184 行开头钩子 + 怎么跑物理锚点 + 私教训练白板类比 + 论断三明治模式 + 3 run-log 逐轮叙事 + 工业偏离 disclaimer + assemble 收尾预告）+ README.md 三段式。**6 条核心论断全部 ✅ 字面命中工业**。**CLAUDE.md 0 假设原则模范执行 + 主动修正 2 处踩坑**。bloom 保持 create（第 11 次巩固完成）。

预判: 已达 assemble 触发条件（artifact_count=12 ≥ 2 + bloom=create ≥ apply + mini harness 7 大子系统 + skill + TodoWrite 主题覆盖完整）→ 询问学生确认拼装 final/ 毕业产物 | 备选: 学生想再加一节（如 plan mode / slash command 系统）→ 推后 assemble

**Socratic 15 评分**（v12 实施前毕业确认 / instructor 先读透 ../claude-code/ TodoWrite 源码再出题）：3 题一次过。Q1（不变量强制）选"只写进 prompt.ts 靠 model 遵守" ✅ —— 抓住 call() 零 validation + TodoListSchema 无 list-level refinement 的软契约本质（types.ts 字面 z.array(TodoItemSchema())）；Q2（抗漂移层）选"fixed-interval 10 轮缺席探测" ✅ —— 切中"缺席才触发 vs 调用才触发"的结构差异，看穿 continuous 层在 model 沉默时根本不会 fire；Q3（持久化+隔离）选"倒扫 messages + key=agentId??sessionId" ✅ —— sessionRestore.ts:77-93 extractTodosFromTranscript 倒扫 + TodoWriteTool.ts:67 todoKey 两机制分清。**instructor 0 假设原则前置执行**：本轮所有论断在出题前已亲自 Read 验证（TodoWriteTool.ts 115 行 / prompt.ts:158 / types.ts 无 refinement / TODO_REMINDER_CONFIG 10/10 / sessionRestore.ts:77-93）。**create 层第 11 次巩固**。应用户要求 v12 由 instructor 实现（参照实现）。

预判: instructor 交付 v12 mini TodoWrite system (artifacts/12-todowrite) → socratic/审阅后触发 assemble | 备选: v12 交付后学生质疑某实现细节 → 源码核实修正

## 卡点记录

- 2026-05-29 TodoWrite 不变量强制层（hint 1/Q2 把"prompt 软契约"误认为"runtime call() validation"）→ 已通过收紧题（verificationNudgeNeeded 同源类推）验证，prompt 协议 vs runtime validation 思维框架内化
